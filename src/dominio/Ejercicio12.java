/**
 * Una expresión lambda consiste en pasar una función como parámetro en un método,
 * de tal forma que podemos aplicar una serie de expresiones para cualquier tipo de función que definamos.
 * En expresiones lambda se trabaja de manera habitual con streams.
 */